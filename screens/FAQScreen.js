import {
    Image,
    StyleSheet,
    Text,
    View,
    TouchableOpacity,
  } from 'react-native';
  
  export default function FAQScreen({ navigation }) {
  
    return (
      <View>
        <Text style={styles.title}>Welcome FAQScreen</Text>
      </View>
    )
  }
  
  const styles = StyleSheet.create({});